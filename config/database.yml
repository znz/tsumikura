# PostgreSQL. Versions 9.5 and up are supported.
#
# development / test は Docker Compose の PostgreSQL コンテナに ENV フォールバックで接続する。
default: &default
  adapter: postgresql
  encoding: unicode
  # For details on connection pooling, see Rails configuration guide
  # https://guides.rubyonrails.org/configuring.html#database-pooling
  max_connections: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  host:     <%= ENV.fetch("DB_HOST",     "localhost") %>
  port:     <%= ENV.fetch("DB_PORT",     5432) %>
  username: <%= ENV.fetch("DB_USERNAME", "tsumikura") %>
  password: <%= ENV.fetch("DB_PASSWORD", "tsumikura") %>

development:
  <<: *default
  database: tsumikura_development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: tsumikura_test

# DATABASE_URL が自動でマージされる (dokku postgres:link が設定する)。
# Solid Queue / Solid Cache も同じ単一 DB を使う (config/cache.yml, config/environments/production.rb)。
production:
  <<: *default
