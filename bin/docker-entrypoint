#!/bin/bash -e

# マイグレーションは app.json の scripts.dokku.predeploy (bundle exec rails db:prepare) に
# 一本化した。新しいイメージにトラフィックを流す前に predeploy が実行されるため、
# ここで db:prepare を実行する必要はない (docs/ops/deployment.md 4節)。

exec "${@}"
